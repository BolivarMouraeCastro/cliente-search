const fs = require('fs');
const https = require('https');

// This script will just dump what's inside Alessandra's folder.
// Since we don't have a valid access token in a local script without OAuth,
// maybe I can't easily run a google API script locally unless I extract the token.
// Actually, it's easier to just broaden the search in the Next.js API!
