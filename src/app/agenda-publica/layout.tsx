export const metadata = {
  title: 'Agenda de Audiências | BM&C Advogados',
  description: 'Consulte a agenda de audiências em tempo real.',
};

export default function AgendaPublicaLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
